[package]
name = "margit"
version.workspace = true
edition = "2024"

[dependencies]
margit-compat = { version = "0.1.0", path = "../margit-compat" }
margit-core = { version = "0.1.0", path = "../margit-core" }
margit-ui = { version = "0.1.0", path = "../margit-ui" }
